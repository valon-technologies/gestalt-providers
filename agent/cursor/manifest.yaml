source: github.com/valon-technologies/gestalt-providers/agent/cursor
version: 0.0.1-alpha.3
displayName: Cursor Agent SDK
description: Agent provider that runs the Cursor Agent SDK with Gestalt MCP catalog tools exposed through a per-turn MCP server.
kind: agent
spec:
  configSchemaPath: schemas/config.schema.yaml
