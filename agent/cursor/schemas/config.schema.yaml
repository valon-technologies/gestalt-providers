type: object
additionalProperties: false
properties:
  defaultModel:
    type: string
    default: composer-2
  timeoutSeconds:
    type: number
    default: 300
    exclusiveMinimum: 0
  systemPrompt:
    type: string
    description: Optional system prompt prepended to every Cursor turn.
  workingDirectory:
    type: string
  cursorApiKey:
    type: string
  sandboxEnabled:
    type: boolean
