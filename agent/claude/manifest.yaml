source: github.com/valon-technologies/gestalt-providers/agent/claude
version: 0.0.1-alpha.1
displayName: Claude Code Agent
description: Agent provider that runs the Claude Code CLI harness with Gestalt tools exposed as an MCP server.
kind: agent
spec:
  configSchemaPath: schemas/config.schema.yaml
