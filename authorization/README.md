# Authorization Providers

Authorization providers for [Gestalt](https://github.com/valon-technologies/gestalt).
Each provider implements the authorization decision plane plus relationship and
model control-plane APIs exposed by `gestaltd`.

See the [configuration guide](https://gestaltd.ai/configuration) for host
configuration and the
[provider development guide](https://gestaltd.ai/providers) for building custom
authorization providers.
