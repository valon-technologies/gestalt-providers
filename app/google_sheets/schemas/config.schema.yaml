type: object
properties:
  clientId:
    type: string
    description: Google OAuth client ID for Google Sheets.
  clientSecret:
    type: string
    description: Google OAuth client secret for Google Sheets.
required:
  - clientId
  - clientSecret
additionalProperties: false
