{
  "description": "Tenant themes must declare these tokens explicitly in :root and .dark. The bundle supplies :where() defaults, but some alias to a different semantic role than Registry / product intent — relying on the default silently mis-maps flyouts and cards.",
  "agentTest": {
    "file": "app/default/src/lib/tenant-theme.contract.test.ts",
    "command": "cd app/default && GESTALT_THEME_FILE=/path/to/deploy/ui/theme.css bun test src/lib/tenant-theme.contract.test.ts"
  },
  "fixture": "shared/fixtures/example-tenant-theme.css",
  "required": {
    ":root": ["--popover", "--popover-foreground"],
    ".dark": ["--popover", "--popover-foreground"]
  },
  "forbiddenValues": {
    "--popover": ["var(--surface-raised)"],
    "--popover-foreground": ["var(--foreground)"]
  },
  "notes": {
    "--popover": "Bundle default is var(--surface-raised). Flyouts should map to page paper / card surface, not raised surface.",
    "--popover-foreground": "Bundle default is var(--foreground). Declare an explicit ink token alongside --popover."
  }
}
