type: object
properties:
  clientId:
    type: string
    description: Google OAuth client ID for Google Calendar.
  clientSecret:
    type: string
    description: Google OAuth client secret for Google Calendar.
required:
  - clientId
  - clientSecret
additionalProperties: false
