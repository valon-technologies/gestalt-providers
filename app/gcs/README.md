# Google Cloud Storage

First-party Gestalt provider plugin for Google Cloud Storage object operations.

REST operations: `objects.list`, `objects.get`, `objects.delete`, `objects.download`, `objects.insert` (media upload via `uploadType=media`).
