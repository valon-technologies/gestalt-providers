# Model Providers

Model providers for [Gestalt](https://github.com/valon-technologies/gestalt).

Each package under `model/` implements stateless model inference for a concrete
backend through Gestalt's portable model provider surface.

Current packages:

- `anthropic`: Python model provider backed by the Anthropic Messages API
