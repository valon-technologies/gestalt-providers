name: Validate Plugins

on:
  pull_request:
    paths:
      - '.github/workflows/**'
      - '.github/scripts/**'
      - 'plugins/**'
      - 'auth/**'
      - 'datastore/**'
      - 'web/**'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/**'
      - '.github/scripts/**'
      - 'plugins/**'
      - 'auth/**'
      - 'datastore/**'
      - 'web/**'

permissions:
  contents: read

env:
  GESTALT_REPOSITORY: valon-technologies/gestalt
  GESTALT_REF: 3e7363564cf9e3be88ba3bc91073a5b777725b97
  VALIDATION_VERSION: 0.0.1-alpha.1
  GOPRIVATE: github.com/valon-technologies/*
  GONOSUMDB: github.com/valon-technologies/*
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"
  UV_PYTHON: "3.14"

jobs:
  discover:
    runs-on: ubuntu-latest
    outputs:
      plugins: ${{ steps.discover.outputs.plugins }}
      count: ${{ steps.discover.outputs.count }}
      scope: ${{ steps.discover.outputs.scope }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Discover plugins to validate
        id: discover
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail

          discovery_output="$(python3 - <<'PY'
          import json
          import os
          import subprocess
          from pathlib import Path

          repo = Path(".")
          package_roots = ("plugins", "auth", "datastore", "web")
          all_plugins = []
          for root_name in package_roots:
              root = repo / root_name
              if not root.is_dir():
                  continue
              for path in sorted(root.glob("*/plugin.yaml")):
                  package_dir = path.parent
                  has_go = any(candidate.suffix == ".go" for candidate in package_dir.glob("*.go"))
                  all_plugins.append(
                      {
                          "dir": str(package_dir.as_posix()),
                          "name": f"{root_name}/{package_dir.name}",
                          "root": root_name,
                          "has_go": has_go,
                          "has_python": (package_dir / "pyproject.toml").is_file(),
                          "has_rust": (package_dir / "Cargo.toml").is_file(),
                      }
                  )
          shared_prefixes = (".github/workflows/", ".github/scripts/")

          event_name = os.environ["EVENT_NAME"]
          run_all = event_name != "pull_request"
          selected = set()

          if event_name == "pull_request":
              base = os.environ["PR_BASE_SHA"]
              head = os.environ["PR_HEAD_SHA"]
              changed_files = subprocess.check_output(
                  ["git", "diff", "--name-only", f"{base}..{head}"],
                  text=True,
              ).splitlines()

              for path in changed_files:
                  if not path:
                      continue
                  if path.startswith(shared_prefixes):
                      run_all = True
                      break
                  parts = Path(path).parts
                  if len(parts) >= 3 and parts[0] in package_roots:
                      package_dir = Path(parts[0]) / parts[1]
                      if (repo / package_dir / "plugin.yaml").is_file():
                          selected.add(package_dir.as_posix())

          if run_all:
              plugins = all_plugins
          else:
              plugins = [plugin for plugin in all_plugins if plugin["dir"] in selected]

          print(
              json.dumps(
                  {
                      "plugins": plugins,
                      "count": len(plugins),
                      "scope": "all" if run_all else "changed",
                  }
              )
          )
          PY
          )"

          plugins_json="$(python3 -c 'import json,sys; print(json.dumps(json.loads(sys.stdin.read())["plugins"]))' <<<"$discovery_output")"
          count="$(python3 -c 'import json,sys; print(json.loads(sys.stdin.read())["count"])' <<<"$discovery_output")"
          scope="$(python3 -c 'import json,sys; print(json.loads(sys.stdin.read())["scope"])' <<<"$discovery_output")"

          {
            echo "plugins<<EOF"
            echo "$plugins_json"
            echo "EOF"
            echo "count=$count"
            echo "scope=$scope"
          } >> "$GITHUB_OUTPUT"

          echo "validation scope: $scope"
          echo "plugin count: $count"
          echo "plugins: $plugins_json"

  build-gestaltd:
    needs: discover
    if: needs.discover.outputs.count != '0'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: Configure private Gestalt repository access
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -euo pipefail

          if [ -z "${PAT_TOKEN}" ]; then
            echo "PAT_TOKEN secret is required to fetch private Gestalt dependencies"
            exit 1
          fi

          cat > "${HOME}/.netrc" <<EOF
          machine github.com
            login x-access-token
            password ${PAT_TOKEN}
          EOF
          chmod 600 "${HOME}/.netrc"

          git config --global url."https://github.com/".insteadOf "ssh://git@github.com/"
          git config --global url."https://github.com/".insteadOf "git@github.com:"
      - name: Install gestaltd
        env:
          GOBIN: ${{ runner.temp }}/bin
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p "$GOBIN"
          echo "$GOBIN" >> "$GITHUB_PATH"
          toolchain_dir="${GITHUB_WORKSPACE}/../gestalt"
          rm -rf "$toolchain_dir"
          if [ -z "${PAT_TOKEN}" ]; then
            echo "PAT_TOKEN secret is required to fetch private Gestalt dependencies"
            exit 1
          fi
          toolchain_url="https://x-access-token:${PAT_TOKEN}@github.com/${GESTALT_REPOSITORY}.git"
          git clone "$toolchain_url" "$toolchain_dir"
          git -C "$toolchain_dir" checkout "${GESTALT_REF}"
          (cd "$toolchain_dir/gestaltd" && go build -o "$GOBIN/gestaltd" ./cmd/gestaltd)
      - uses: actions/upload-artifact@v4
        with:
          name: gestaltd
          path: ${{ runner.temp }}/bin/gestaltd
          if-no-files-found: error

  validate:
    needs:
      - discover
      - build-gestaltd
    if: needs.discover.outputs.count != '0'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        plugin: ${{ fromJson(needs.discover.outputs.plugins) }}
    name: Validate ${{ matrix.plugin.name }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
        if: matrix.plugin.has_python
      - uses: dtolnay/rust-toolchain@1.88.0
        if: matrix.plugin.has_rust
      - uses: actions/setup-node@v4
        if: matrix.plugin.root == 'web'
        with:
          node-version: '25'
      - uses: actions/setup-go@v5
        if: matrix.plugin.has_go
        with:
          go-version: '1.26'
      - name: Install Python
        if: matrix.plugin.has_python
        run: uv python install 3.14
      - name: Configure private Gestalt repository access
        if: matrix.plugin.has_go || matrix.plugin.has_python || matrix.plugin.has_rust || matrix.plugin.root == 'web'
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -euo pipefail

          if [ -z "${PAT_TOKEN}" ]; then
            echo "PAT_TOKEN secret is required to fetch private Gestalt dependencies"
            exit 1
          fi

          cat > "${HOME}/.netrc" <<EOF
          machine github.com
            login x-access-token
            password ${PAT_TOKEN}
          EOF
          chmod 600 "${HOME}/.netrc"

          git config --global url."https://github.com/".insteadOf "ssh://git@github.com/"
          git config --global url."https://github.com/".insteadOf "git@github.com:"
      - name: Prepare sibling Gestalt checkout for local source packaging
        if: matrix.plugin.has_go || matrix.plugin.has_python || matrix.plugin.root == 'web'
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -euo pipefail
          toolchain_dir="${GITHUB_WORKSPACE}/../gestalt"
          rm -rf "$toolchain_dir"
          if [ -z "${PAT_TOKEN}" ]; then
            echo "PAT_TOKEN secret is required to fetch private Gestalt dependencies"
            exit 1
          fi
          toolchain_url="https://x-access-token:${PAT_TOKEN}@github.com/${GESTALT_REPOSITORY}.git"
          git clone "$toolchain_url" "$toolchain_dir"
          git -C "$toolchain_dir" checkout "${GESTALT_REF}"
      - uses: actions/download-artifact@v4
        with:
          name: gestaltd
          path: ${{ runner.temp }}/bin
      - name: Add gestaltd to PATH
        run: |
          chmod +x "${RUNNER_TEMP}/bin/gestaltd"
          echo "${RUNNER_TEMP}/bin" >> "$GITHUB_PATH"
      - name: Validate plugin
        env:
          OUTPUT_ROOT: ${{ runner.temp }}/dist
        run: |
          set -euo pipefail
          plugin_dir="${{ matrix.plugin.dir }}"
          plugin_name="${{ matrix.plugin.name }}"

          mkdir -p "$OUTPUT_ROOT"

          if [ "${{ matrix.plugin.has_go }}" = "true" ]; then
            (cd "$plugin_dir" && go test ./...)
          fi

          if [ "${{ matrix.plugin.has_rust }}" = "true" ]; then
            . .github/scripts/setup_rust_datastore_test_env.sh "$plugin_dir"
            (cd "$plugin_dir" && cargo test)
          fi

          if [ -f "${plugin_dir}/pyproject.toml" ]; then
            python3 .github/scripts/validate_python_plugin.py "$plugin_dir"
          fi

          if [ "${{ matrix.plugin.root }}" = "web" ]; then
            export GESTALT_CHECKOUT="${GITHUB_WORKSPACE}/../gestalt"
          fi

          rm -rf "${OUTPUT_ROOT:?}/${plugin_name}"
          mkdir -p "${OUTPUT_ROOT}/${plugin_name}"
          (cd "$plugin_dir" && gestaltd provider release --version "${VALIDATION_VERSION}" --output "${OUTPUT_ROOT}/${plugin_name}")

          if ! find "${OUTPUT_ROOT}/${plugin_name}" -mindepth 1 -maxdepth 1 -type f | grep -q .; then
            echo "ERROR: ${plugin_name} produced no packaged artifacts"
            exit 1
          fi
