source: github.com/valon-technologies/gestalt-providers/secrets/relationaldb
version: 0.0.1-alpha.1
displayName: Relational Database Secrets
description: Resolves KMS-encrypted deployment secrets from a relational database.
kind: secrets
install:
  command: [go, mod, download]
build:
  command: [go, run, github.com/valon-technologies/gestalt/sdk/go/cmd/gestalt, build]
run:
  command: [go, run, github.com/valon-technologies/gestalt/sdk/go/cmd/gestalt, run]
spec: {}
