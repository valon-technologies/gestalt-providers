# Cache Providers

Cache providers for [Gestalt](https://github.com/valon-technologies/gestalt).
Each provider implements the cache interface used by plugins for short-lived
key/value state.

See the [configuration guide](https://gestaltd.ai/configuration) for setting up
cache bindings and the
[provider development guide](https://gestaltd.ai/providers) for writing custom
cache providers.
