---
title: Events
---

# Slack Events

Slack event handling always uses the workflow manager for durable per-thread
dispatch.

## Agent Replies

For the common case where one Slack app has one default bot behavior, configure
the agent once:

```yaml
plugins:
  slack:
    source: https://github.com/valon-technologies/gestalt-providers/releases/download/plugins/slack/v0.0.1-alpha.N/provider-release.yaml
    authorizationPolicy: platform
    invokes:
      - plugin: slack
        operation: events.reply
        credentialMode: none
      - plugin: slack
        operation: events.setStatus
        credentialMode: none
      - plugin: slack
        operation: events.deleteStatus
        credentialMode: none
      - plugin: slack
        operation: events.addReaction
        credentialMode: none
      - plugin: slack
        operation: events.removeReaction
        credentialMode: none
      - plugin: slack
        operation: events.setAssistantStatus
        credentialMode: none
      - plugin: slack
        operation: events.clearAssistantStatus
        credentialMode: none
      - plugin: slack
        operation: events.setThreadTitle
        credentialMode: none
      - plugin: slack
        operation: events.setSuggestedPrompts
        credentialMode: none
      - plugin: slack
        operation: interactions.request
        credentialMode: none
      - plugin: slack
        operation: conversations.getThreadContext
      - plugin: slack
        operation: files.get
      - plugin: companyDirectory
        operation: lookupProfile
    config:
      bot:
        token:
          secret:
            provider: secrets
            name: slack-bot-token
      acknowledgement:
        reaction: eyes
      agent:
        provider: simple
        model: deep
        systemPrompt: Use Slack formatting and keep replies concise.
        threadContext:
          enabled: true
          maxMessages: 200
        tools:
          - plugin: companyDirectory
            operation: lookupProfile
      assistant:
        enabled: true
        status: is checking that...
        loadingMessages:
          - Reading the Slack thread
          - Calling available tools
        iconEmoji: ":hourglass_flowing_sand:"
        suggestedPrompts:
          title: Try next
          prompts:
            - title: Summarize this thread
              message: Summarize this thread and call out open questions.
```

## Workflow Dispatch

Slack event handling always uses the workflow manager for durable per-thread
dispatch:

```yaml
plugins:
  slack:
    config:
      workflow:
        provider: local
      agent:
        provider: simple
        model: deep
        threadContext:
          enabled: true
          maxMessages: 200
```

`events.handle` calls `WorkflowManager.SignalOrStartRun` with
`provider_name=workflow.provider`, a thread-scoped workflow key, and a
`slack.event` signal. Later Slack messages in the same thread signal the
existing keyed run instead of replacing its target or authorization context.

When `acknowledgement.reaction` is configured, `events.handle` adds that
reaction to the source Slack message only after the workflow signal-or-start
call succeeds. Emoji names may be written with or without colons, and Slack's
`already_reacted` response is treated as idempotent success.

For workflow-dispatched `message` and `app_mention` events that include
`thread_ts`, `events.handle` fetches one bounded page of thread replies with the
configured bot token before signaling the workflow. On success, the signal
payload includes `slack.thread_context` and the generated `user_prompt` includes
a prefetched thread context section. If Slack returns an API/client error, the
workflow still receives the event with `slack.thread_context_error` and the
thread-context helper remains available as a fallback tool.

The default prefetch limit is 200 messages and `agent.threadContext.maxMessages`
is clamped to Slack's 1-1000 page-size range. Some Slack apps have lower
`conversations.replies` limits; set a lower `maxMessages` or disable prefetch
with `agent.threadContext.enabled: false` for those deployments. Prefetch can
also be tuned with `includeUserInfo`, `includeBots`, `includeFiles`,
`includeFileContent`, `includeImageData`, and `maxFileBytes`.

Route-level `agent.routes[].agent.threadContext` overrides the global thread
context settings for matching events. If omitted, the route inherits the global
settings. If `enabled: false` is set on the route, prefetch is disabled for that
route even when it is enabled globally.

## Durable Handoff

`events.handle` uses the workflow provider as the durable boundary. It signs a
short-lived `reply_ref`, calls `WorkflowManager.SignalOrStartRun`, and only then
acknowledges the Slack callback. If the workflow handoff fails, the handler
returns an error so Slack can retry the same event. The workflow provider owns
run state, signal idempotency, retries, and output delivery.

After the workflow has accepted the event, Slack-visible acknowledgement
reactions and native assistant loading status updates are best effort. Failures
there are reported in the response body but do not block the Slack callback,
because the durable workflow has already accepted the event.

## HTTP Routes

Slack should send Events API requests to:

```text
POST /api/v1/slack/event
```

Slack should send interactivity requests to:

```text
POST /api/v1/slack/interactions
```

Both routes are declared in `manifest.yaml` under `spec.http`, validate Slack
HMAC signatures with `SLACK_SIGNING_SECRET`, and resolve the Slack team/user
through the managed `external_identity` authorization relationship.

## Hidden Helper Operations

`events.handle`, `events.reply`, `events.setStatus`, `events.deleteStatus`,
`events.addReaction`, `events.removeReaction`, the native assistant helpers, the
streaming helpers, and the interaction helpers are hidden operations.

`events.handle` starts or signals a keyed workflow run and passes an opaque
`reply_ref` in the signal payload. The workflow runtime invokes `events.reply`
using the configured output delivery binding. The same `reply_ref` scopes
progress statuses, native assistant updates, suggested prompts, thread titles,
native streaming replies, reactions, and interactions to the source event
channel, so the agent never needs raw `chat.postMessage` access for event
replies.

Runtime output-delivery input:

```json
{"reply_ref":"...","text":"I'll check that now."}
```

Native streaming helper input:

```json
{"reply_ref":"...","markdown_text":"Working on it..."}
```

Use the returned stream timestamp with `events.appendStream` and
`events.stopStream`:

```json
{"reply_ref":"...","stream_ts":"1712161830.000400","markdown_text":"Done."}
```

## Agent Routes

Use `agent.routes` when one Slack app should dispatch different workflows,
assistants, prompts, tools, acknowledgement reactions, or thread context
behavior for different channels or event types.

```yaml
plugins:
  slack:
    config:
      workflow:
        provider: local
      agent:
        provider: simple
        model: deep
        toolSets:
          directory:
            - plugin: companyDirectory
              operation: lookupProfile
          status:
            - plugin: statusPage
              operation: getStatus
        toolSetRefs:
          - directory
        tools:
          - plugin: tickets
            operation: search
        routes:
          - id: team-help
            match:
              channels:
                - C0123456789
              eventTypes:
                - app_mention
                - message
            agent:
              acknowledgement:
                reaction: eyes
              systemPrompt: Help with team questions.
          - id: operations-help
            match:
              channels:
                - C9876543210
              eventTypes:
                - app_mention
                - assistant_thread_started
                - assistant_thread_context_changed
            workflow:
              provider: operations
            agent:
              assistant:
                enabled: true
                status: is checking status...
                suggestedPrompts:
                  title: Try next
                  prompts:
                    - title: Summarize status
                      message: Summarize the latest service status.
              acknowledgement:
                enabled: false
              threadContext:
                maxMessages: 50
                includeFiles: false
              toolSetRefs:
                - status
              systemPrompt: Help engineers inspect service status.
```

Route settings inherit from the top-level provider configuration when omitted.
Set `enabled: false` on route `agent.assistant`, `agent.acknowledgement`, or
`agent.threadContext` to explicitly disable an inherited global setting. Route
`workflow.provider` overrides the global `workflow.provider` for Slack events
and Slack interaction button callbacks generated from that route.

Tool sets are named groups under `agent.toolSets`. Tool references expand in
this order and deduplicate by exact plugin/operation/connection/instance with
first reference winning: top-level `agent.toolSetRefs`, top-level
`agent.tools`, route `agent.toolSetRefs`, route `agent.tools`, then Slack
helper operations.

When a route uses `match.eventTypes`, include
`assistant_thread_started` and `assistant_thread_context_changed` on routes that
should handle native Slack assistant suggested prompts. For native assistant
thread events, `match.channels` matches the active Slack context channel when
Slack includes it; assistant API calls still use Slack's assistant thread
channel internally. Signed interaction callbacks include the route ID; if a
non-empty signed route ID no longer exists in the provider configuration, the
provider rejects the callback instead of silently falling back to global
behavior.

## Event Publishing

To publish Slack Events API callbacks directly into Gestalt workflow events,
configure `events.publish.routes`. Publish routes are independent from
`agent.routes`; they do not require a linked Slack user or the Slack bot token.

```yaml
plugins:
  slack:
    source: https://github.com/valon-technologies/gestalt-providers/releases/download/plugins/slack/v0.0.1-alpha.N/provider-release.yaml
    authorizationPolicy: platform
    config:
      events:
        publish:
          routes:
            - id: message-events
              workflowProvider: local
              workflowEventType: slack.event.received
              source: slack
              subject: route:message-events
              match:
                eventTypes:
                  - message
                subtypes: []
                channelIds:
                  - C0123456789
```

`workflowProvider` selects the Gestalt workflow provider that should receive the
published event; if omitted, the route uses the top-level `workflow.provider`
when configured, otherwise Gestalt publishes through the workflow manager's
default behavior. `workflowEventType` defaults to `slack.event.received`,
`source` defaults to `slack`, and `subject` defaults to `route:<routeId>`.
