type: object
properties:
  org_id:
    type: string
    description: Modern Treasury organization ID used as the Basic auth username.
  base_url:
    type: string
    description: Base API URL. Override only for non-default environments.
    default: https://app.moderntreasury.com/api
required:
  - org_id
additionalProperties: false
