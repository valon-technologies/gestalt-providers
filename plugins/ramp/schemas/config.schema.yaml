type: object
properties:
  client_id:
    type: string
    description: Ramp OAuth client ID.
  client_secret:
    type: string
    description: Ramp OAuth client secret.
required:
  - client_id
  - client_secret
additionalProperties: false
