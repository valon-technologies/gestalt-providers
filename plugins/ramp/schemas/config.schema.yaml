type: object
properties:
  clientId:
    type: string
    description: Ramp OAuth client ID.
  clientSecret:
    type: string
    description: Ramp OAuth client secret.
required:
  - clientId
  - clientSecret
additionalProperties: false
