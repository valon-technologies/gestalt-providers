type: object
properties:
  clientId:
    type: string
    description: Google OAuth client ID for Google Drive.
  clientSecret:
    type: string
    description: Google OAuth client secret for Google Drive.
required:
  - clientId
  - clientSecret
additionalProperties: false
