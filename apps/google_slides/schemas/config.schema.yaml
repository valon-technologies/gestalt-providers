type: object
properties:
  clientId:
    type: string
    description: Google OAuth client ID for Google Slides.
  clientSecret:
    type: string
    description: Google OAuth client secret for Google Slides.
required:
  - clientId
  - clientSecret
additionalProperties: false
