# Test Provider

`test/hello-world` is a deliberately minimal `kind: test` provider used to
exercise the provider-kind registration, packaging, release, and consumption
workflow.

The provider exposes a single `HelloWorld` RPC and always returns `HelloWorld`.
